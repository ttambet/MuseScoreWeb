{
    "tasks": [
        {
            "type": "shell",
            "label": "Build web",
            "command": "qmake ../libmscore.pro && make -j8 2>&1 | tee build.log",
            "options": {
                "cwd": "${workspaceFolder}/build.web"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}